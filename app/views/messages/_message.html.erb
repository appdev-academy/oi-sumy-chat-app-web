<div id="<%= dom_id message %>" class="w-full sm:w-auto space-y-5">
  Sent by <strong class="font-medium"><%= message.user.username %></strong> at <%= message.created_at.in_time_zone("Europe/Kyiv").strftime("%H:%M") %>
  <p><%= message.content %></p>
</div>
